import { EventEmitter } from '../../stencil-public-runtime';
import { ModusDataTableCellLink, ModusDataTableDisplayOptions, ModusDataTableRowAction, ModusDataTableRowActionClickEvent, ModusDataTableSort, ModusDataTableSortEvent, ModusDataTableSelectionOptions, ModusDataTableSortOptions, TCell, TColumn, TRow } from './modus-data-table.models';
export declare class ModusDataTable {
    columns: string[] | TColumn[];
    data: TCell[][] | TRow[];
    dataChanged(_: any, oldValue: TCell[][] | TRow[]): void;
    /** Options for data table display. */
    displayOptions?: ModusDataTableDisplayOptions;
    /** Actions that can be performed on each row. */
    rowActions?: ModusDataTableRowAction[];
    /** Options for data table item selection. */
    selectionOptions?: ModusDataTableSelectionOptions;
    /** Options for data table column sort. */
    sortOptions?: ModusDataTableSortOptions;
    /** An event that fires on cell link click. */
    cellLinkClick: EventEmitter<ModusDataTableCellLink>;
    /** An event that fires on row double click. */
    rowDoubleClick: EventEmitter<string>;
    /** An event that fires on selection change. */
    selection: EventEmitter<string[]>;
    /** An event that fires on column sort. */
    sort: EventEmitter<ModusDataTableSortEvent>;
    /** An event that fires when a row action is clicked. */
    rowActionClick: EventEmitter<ModusDataTableRowActionClickEvent>;
    allSelected: boolean;
    sortState: ModusDataTableSort;
    classBySize: Map<string, string>;
    originalData: TRow[];
    componentWillLoad(): void;
    componentDidLoad(): void;
    componentWillUpdate(): void;
    convertColumnsAndRows(): void;
    emitSelection(): void;
    handleCheckboxClick(rowId: string): void;
    handleColumnHeaderClick(columnId: string): void;
    handleHeaderCheckboxClick(selectAll: boolean): void;
    handleRowClick(rowId: string): void;
    handleRowDoubleClick(rowId: string): void;
    updateAllSelected(): void;
    render(): unknown;
}
