import { EventEmitter, // eslint-disable-line @typescript-eslint/no-unused-vars
JSX } from '../../stencil-public-runtime';
import { PaginationDirection } from './enums/pagination-direction.enum';
export declare class ModusPagination {
    ariaLabel: string | null;
    activePage: number;
    activePageWatch(newValue: number, oldValue: number): void;
    maxPage: number;
    minPage: number;
    prevPageButtonText?: string;
    nextPageButtonText?: string;
    size: 'large' | 'medium' | 'small';
    /** An event that fires on page change. */
    pageChange: EventEmitter<number>;
    pages: (number | string)[];
    constructor();
    componentWillRender(): void;
    chevronSizeBySize: Map<string, string>;
    classBySize: Map<string, string>;
    setPages(): void;
    handleChevronClick(direction: PaginationDirection): void;
    handleChevronKeydown(event: KeyboardEvent, direction: PaginationDirection): void;
    handlePageKeydown(event: KeyboardEvent, page: number): void;
    handlePageClick(page: number): void;
    renderPreviousPageControl(): JSX.Element[];
    renderNextPageControl(): JSX.Element[];
    renderPageNumbers(): JSX.Element[];
    render(): unknown;
}
