export type token = 'm' | 'mm' | 'mmm' | 'mmmm' | 'd' | 'dd' | 'yy' | 'yyyy';
export type tokenType = 'month' | 'date' | 'year';
type tokens = {
    [k in token]: {
        regex: string;
        type: tokenType;
    };
};
type formats = {
    [k in token]: (val: number, defaultValue?: number) => string;
};
type parsers = {
    [k in token]: (val: string, defaultValue?: number) => number;
};
export declare const Tokens: tokens;
export declare const TokenFormatting: formats;
export declare const TokenParser: parsers;
export {};
