import { EventEmitter } from '../../stencil-public-runtime';
import { ButtonGroupSelectionType } from './modus-button-group.models';
import { ButtonColor, ButtonSize, ButtonStyle, ButtonType } from '../modus-button/modus-button.models';
export declare class ModusButtonGroup {
    /** Array to store selected buttons */
    selectedButtons: HTMLModusButtonElement[];
    host: HTMLElement;
    /** (optional) The button group's aria-disabled state. */
    ariaDisabled: string | null;
    /** (optional) The button group's aria-label. */
    ariaLabel: string | null;
    /** (optional) The style of the buttons in the group */
    buttonStyle: ButtonStyle;
    /** (optional) The color of the buttons in the group */
    color: ButtonColor;
    /** (optional) Disables the button group. */
    disabled: boolean;
    /** (optional) The selection type of buttons */
    selectionType: ButtonGroupSelectionType;
    selectionTypeChanged(newValue: ButtonGroupSelectionType): void;
    /** (optional) The size of the buttons */
    size: ButtonSize;
    protected disabledChanged(): void;
    protected sizeChanged(): void;
    /** Event emitted when a button is clicked */
    buttonGroupClick: EventEmitter<unknown>;
    /** Event emitted when the selection changes */
    buttonSelectionChange: EventEmitter<HTMLModusButtonElement[]>;
    componentWillLoad(): void;
    handleSlotChange(): void;
    handleButtonClick(event: CustomEvent): void;
    setupButtons(reset?: boolean): void;
    renderButtons(buttons: NodeListOf<HTMLModusButtonElement>, reset: boolean): void;
    determineButtonType(): ButtonType;
    toggleMultiSelect(clickedButton: HTMLModusButtonElement): void;
    toggleSingleSelect(clickedButton: HTMLModusButtonElement): void;
    render(): any;
}
