import { EventEmitter } from '../../../stencil-public-runtime';
export interface ModusNavbarApp {
    description?: string;
    logoUrl: string;
    name: string;
    url: string;
    category?: string;
    showCategory?: boolean;
}
export declare class ModusNavbarAppsMenu {
    apps: ModusNavbarApp[];
    reverse: boolean;
    appOpen: EventEmitter<ModusNavbarApp>;
    clickAppHandler(event: MouseEvent, app: ModusNavbarApp): void;
    render(): unknown;
}
