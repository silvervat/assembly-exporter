import { ModusToolTipPlacement } from './modus-tooltip.models';
export declare class ModusTooltip {
    element: HTMLElement;
    /** (optional) The tooltip's aria-label. */
    ariaLabel: string | null;
    /** (optional) The tooltip's position relative to its content. */
    position: ModusToolTipPlacement;
    handlePositionChange(newValue: ModusToolTipPlacement): void;
    /** The tooltip's text. */
    text: string;
    onTextChange(newValue: string): void;
    /** Hide the tooltip */
    disabled: boolean;
    onDisabledChange(newValue: boolean): void;
    private popperInstance;
    private tooltipElement;
    private readonly showEvents;
    private readonly hideEvents;
    private hoverTimer;
    private showEventsListener;
    private hideEventsListener;
    private attachEventListeners;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    initializePopper(position: ModusToolTipPlacement): void;
    cleanupPopper(): void;
    show(): void;
    hide(): void;
    render(): unknown;
}
