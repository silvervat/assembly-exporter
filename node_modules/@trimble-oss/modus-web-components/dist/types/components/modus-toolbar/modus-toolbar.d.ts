export declare class ModusToolbar {
    /** (optional) The button's aria-label. */
    ariaLabel: string | null;
    /** (optional) Disables the button. */
    disabled: boolean;
    host: HTMLElement;
    createButton(child: Element): any;
    renderButtons(): any[];
    render(): any;
}
