import { h, // eslint-disable-line @typescript-eslint/no-unused-vars
 } from "@stencil/core";
export const ModusTableCell = ({ cell, cellIndex, context, valueChange }) => {
    const { rowsExpandable, frozenColumns } = context;
    const hasRowsExpandable = cellIndex === 0 && rowsExpandable;
    const { id } = cell;
    return (h("td", { key: id, tabindex: 0, class: `
      ${hasRowsExpandable || frozenColumns.includes(cell.column.id) ? 'sticky-left' : ''}
      ${cell.column.getIsResizing() ? 'active-resize' : ''}
  `, style: { width: `${cell.column.getSize()}px` } }, h("modus-table-cell-main", { cell: cell, hasRowsExpandable: hasRowsExpandable, context: context, valueChange: valueChange })));
};
