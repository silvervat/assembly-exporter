export const DEFAULT_CONTAINER_LAYOUT = Object.freeze({
    position: 'fixed',
    top: '0px',
    left: '0px',
    height: '0px',
});
export const DOM_OBSERVER_CONFIG = Object.freeze({
    childList: true,
});
