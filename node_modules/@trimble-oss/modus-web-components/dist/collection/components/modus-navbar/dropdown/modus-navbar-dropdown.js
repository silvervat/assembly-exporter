import { h, // eslint-disable-line @typescript-eslint/no-unused-vars
 } from "@stencil/core";
export const ModusNavbarDropdown = ({ itemSelect, options, reverse, selectedItem }) => {
    const direction = reverse ? 'rtl' : 'ltr';
    const toggleElementId = 'navbar-dropdown';
    const itemSelectHandler = (item) => {
        itemSelect(item);
    };
    return (h("modus-dropdown", { "toggle-element-id": toggleElementId }, h("modus-button", { "aria-label": options.ariaLabel, "button-style": 'borderless', color: 'secondary', id: toggleElementId, slot: 'dropdownToggle', "show-caret": true }, selectedItem === null || selectedItem === void 0 ? void 0 : selectedItem.text), h("modus-list", { dir: direction, slot: 'dropdownList' }, options.items.map((item) => (h("modus-list-item", { key: item.value, onItemClick: () => itemSelectHandler(item), selected: item === selectedItem }, item.text))))));
};
