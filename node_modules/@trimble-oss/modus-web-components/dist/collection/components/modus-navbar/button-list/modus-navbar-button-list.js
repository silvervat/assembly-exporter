import { h, // eslint-disable-line @typescript-eslint/no-unused-vars
 } from "@stencil/core";
import { ModusIconMap } from "../../../icons/ModusIconMap";
export const ModusNavbarButtonList = ({ buttons, reverse, onClick, onKeyDown, openButtonMenuId }) => {
    const navbarButtons = buttons || [];
    return navbarButtons.map((button, index) => {
        var _a;
        return (h("div", { onClick: (event) => onClick(event, button), onKeyDown: (event) => onKeyDown(event, button) }, h("modus-dropdown", { "toggle-element-id": 'navbar-button-' + index, showDropdownListBorder: false }, h("div", { class: "navbar-button", id: 'navbar-button-' + index, slot: "dropdownToggle" }, h("span", { class: "navbar-button-icon", role: "button", tabIndex: 0 }, h("modus-tooltip", { text: (_a = button.tooltip) === null || _a === void 0 ? void 0 : _a.text, position: "bottom" }, h("div", { class: "icon-button" }, h(ModusIconMap, { icon: button.icon, size: "24", pressed: openButtonMenuId === button.id }))))), !button.hideMenu && (h("modus-navbar-button-menu", { slot: "dropdownList", reverse: reverse }, h("slot", { name: button.id }))))));
    });
};
