// eslint-disable-next-line
import { h } from "@stencil/core";
export const ModusNavbarProductLogo = ({ logos, onClick }) => {
    var _a, _b;
    const { primary, secondary } = logos || {};
    const handleKeyDown = (event) => {
        if (event.key !== 'Enter' && event.key !== ' ') {
            return;
        }
        if (onClick) {
            onClick(event);
        }
    };
    return (h("div", { "aria-label": "Logo", onClick: onClick, onKeyDown: handleKeyDown, tabindex: "0", role: "button", class: "product-logo" }, primary && (h("img", { class: secondary && 'product-logo-primary', height: (_a = primary.height) !== null && _a !== void 0 ? _a : '24', src: primary.url, alt: "Modus Navbar primary product logo", "data-test-id": "primary-logo" })), secondary && (h("img", { class: primary && 'product-logo-secondary', height: (_b = secondary.height) !== null && _b !== void 0 ? _b : '24', src: secondary.url, alt: "Modus Navbar secondary product logo", "data-test-id": "secondary-logo" }))));
};
