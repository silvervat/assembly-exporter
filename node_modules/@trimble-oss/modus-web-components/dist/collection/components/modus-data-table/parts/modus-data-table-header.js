// eslint-disable-next-line
import { h } from "@stencil/core";
import { IconSortAZ } from "../../../icons/svgs/icon-sort-a-z";
import { IconSortZA } from "../../../icons/svgs/icon-sort-z-a";
export function convertToSingleSpaceTitleCase(title) {
    return title === null || title === void 0 ? void 0 : title.replace(/\w\S*/g, (word) => {
        return word.charAt(0).toUpperCase() + word.substring(1).toLowerCase();
    }).replace(/\s+/g, ' ');
}
export const ModusDataTableHeader = (props) => {
    const sortIcon = props.sortState.direction !== 'none' ? (h("modus-tooltip", { position: "bottom", text: `${props.sortState.direction === 'asc' ? 'Sort descending' : 'Remove sort'}` }, h("div", { class: "icon-container" }, h("div", { class: "sort-icon" }, props.sortState.direction === 'asc' ? h(IconSortAZ, { size: '16' }) : h(IconSortZA, { size: '16' }))))) : null;
    const sortIconPlaceholder = h("div", { style: { width: '16px' } });
    return (h("th", { class: `${props.sortOptions.canSort ? 'can-sort' : ''}`, onClick: () => props.onColumnHeaderClick(props.column.id) }, h("div", { class: `column-header align-${props.column.align}` }, props.column.align === 'right' && props.sortState.columnId !== props.column.id && sortIconPlaceholder, props.column.align === 'right' &&
        props.sortState.columnId === props.column.id &&
        props.sortState.direction === 'none' &&
        sortIconPlaceholder, props.column.align === 'right' && props.sortState.columnId === props.column.id && sortIcon, h("div", null, convertToSingleSpaceTitleCase(props.column.display)), props.column.align === 'left' && props.sortState.columnId === props.column.id && sortIcon, props.column.align === 'center' && props.sortState.columnId === props.column.id && (h("div", { style: { position: 'relative' } }, h("div", { style: { display: 'flex', justifyContent: 'flex-end', position: 'absolute' } }, sortIcon))))));
};
