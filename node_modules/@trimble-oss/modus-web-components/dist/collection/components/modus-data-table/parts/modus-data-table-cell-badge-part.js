// eslint-disable-next-line
import { h } from "@stencil/core";
export const ModusDataTableCellBadgePart = (props) => {
    return (h("div", { class: "cell-badge" }, h("modus-badge", { color: props.badge.color, type: props.badge.type }, props.badge.text)));
};
