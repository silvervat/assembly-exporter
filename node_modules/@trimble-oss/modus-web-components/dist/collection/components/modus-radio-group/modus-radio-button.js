// eslint-disable-next-line
import { h } from "@stencil/core";
export const ModusRadioButton = (props) => (h("div", { class: `modus-radio-button ${props.disabled ? 'disabled' : ''}` }, h("div", { class: `radio ${props.size}`, tabIndex: 0, onClick: () => props.handleButtonClick(props.id), onKeyDown: (event) => props.handleKeydown(event, props.id) }, h("input", { id: `radio-${props.id}`, checked: props.checked, disabled: props.disabled, name: props.name, type: "radio" }), h("span", { class: `checkmark` }), h("label", { htmlFor: `radio-${props.id}` }, props.label))));
