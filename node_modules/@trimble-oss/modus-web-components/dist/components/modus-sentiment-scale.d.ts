import type { Components, JSX } from "../types/components";

interface ModusSentimentScale extends Components.ModusSentimentScale, HTMLElement {}
export const ModusSentimentScale: {
    prototype: ModusSentimentScale;
    new (): ModusSentimentScale;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
